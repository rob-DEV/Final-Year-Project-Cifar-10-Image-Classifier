 # for w in range(outWidth):
        #     for h in range(outWidth):
        #         for i in range(3):
        #             for j in range(3):
        #                 gs_pixel = grayscale[w, h]
        #                 msk_val = horizontial_mask[i, j]
        #                 out_val = outImage[w, h] + (gs_pixel * msk_val)
        #                 outImage[w, h] = max(out_val, 0)